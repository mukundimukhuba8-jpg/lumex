Binary PNG assets could not be recovered from the transcript.

Checked-in files under `assets/` are **1×1 placeholder PNGs** so `require('...png')` resolves.
Replace especially `lumex-bot.png` with the real brand/bot artwork before any UI review.
